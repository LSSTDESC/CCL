language: python

python:
  - "2.7"
  - "3.6"

addons:
  apt:
    packages:
      - fftw3
      - fftw3-dev
      - pkg-config
      - libgsl0-dev
      - swig
      - python-numpy
      - python3-numpy
      - cmake

install: true

script:
    - cmake --version
    - python setup.py build
    - python setup.py test
    - sudo make -Cbuild install
    - check_ccl
